# Given a df of killmap, this function generates a subsumption graph
# It uses a graph library to visualize the subsumption graph

import pandas as pd


def subsumption_graph_generator(killmap):
    # TODO test this function
    '''
    Given a df of killmap, this function generates a subsumption graph
    It uses a graph library to visualize the subsumption graph
    '''


if __name__ == "__main__":
    killmap = pd.read_csv("./mutation/reports/mutation/killmap.csv")
